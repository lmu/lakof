[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]

[packages]
pre-commit = "==2.6.0"
"zc.buildout" = "==2.13.3"
# TODO
# https://github.com/pypa/pipenv/issues/2364
# Force install with pipenv install "setuptools==42.0.2"
# LAST VERSION FOR PYTHON2
setuptools = "==42.0.2"

[requires]
python_version = "3.8"
